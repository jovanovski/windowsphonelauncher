<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The face every window, dialog and menu is lettered in. -->
    <attr name="primaryFontFamily" format="reference" />

    <!-- Windows Phone 8.1 shell.
         Defined on the base theme too, so wp81_* drawables and layouts still resolve
         if they are ever inflated under another theme. Live accent and Light/Dark
         switching is applied programmatically, not through these attributes. -->
    <attr name="wpAccent" format="color" />
    <attr name="wpBackground" format="color" />
    <attr name="wpForeground" format="color" />
    <attr name="wpForegroundSubtle" format="color" />

</resources>